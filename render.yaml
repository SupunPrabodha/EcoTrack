services:
  - type: web
    name: ecotrack-backend
    env: node
    rootDir: backend
    buildCommand: corepack prepare pnpm@10.29.3 --activate && corepack pnpm install --frozen-lockfile
    startCommand: corepack pnpm start
